import React from 'react';
import { View, ActivityIndicator } from 'react-native';

const Loading = props => {
    return (
        <View style={{ }}><ActivityIndicator size="large" /></View>
    )
}

export { Loading };